# 軽量版pre-commit: 変更ファイルのみを高速チェック
# 完全チェック(TypeScript/Lint/Build)はCI/CDで実行

echo "🚀 変更ファイルの基本チェックを実行中..."

# Lint-stagedで変更ファイルのみをチェック
npx lint-staged

if [ $? -ne 0 ]; then
  echo "❌ 変更ファイルのチェックに失敗しました。修正してからコミットしてください。"
  exit 1
fi

echo "✅ 基本チェック完了！詳細チェック(TypeScript/Build)はCI/CDで実行されます。"
